#include <stdio.h>

/* This is a comment. */
int main(int argc, char *argv[])
{
    int distance = 100;

    // this is also a comment
    printf("You are %d miles away.\n", distance);
    printf("But you are not lost.\n");
    printf("\t\tYou are learning C.\n");

    return 0;
}
